prep {
    ingredient _x := 0;
    serve("Started");
    eightysix("Error Entered", Exception);
} rescue (Exception e) {
    serve(e);
} rescue (ValueError v) {
    serve(v);
} cleanup {
    serve("Finished");
}
