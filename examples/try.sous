prep {
    ingredient _x := 0;
    serve("Started");
    eightysix("Error Entered", TypeError);
} rescue (Exception e) {
    serve(e.stack);
} cleanup {
    serve("Finished");
}

'''
rescue (ValueError v) {
    serve(v.stack);
} 
'''