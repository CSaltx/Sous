'''
// Dish S {} S x:=new S(); serve(x.y);

Dish S {
    ingredient x : int;
    recipe bake() : int {
        serve(x);
    }
}

S test := new S(5);

test.bake();


recipe x (ingredient y: int) : int {
    return 5;
}
'''

ingredient x := 5, y := 2, z := "s";
serve("z:", z);


for (ingredient i := 0; i < 50; ++i) {
    serve(i);
}

'''
// SHOULD THIS BE ALLOWED OR NO?
for (ingredient i := 0; i < 50; ++i) {
    serve(i);
}
'''