ingredient temp : int;

recipe CheckTemperature() : int{
    if (temp > 300) {
        serve("Temperature is hot enough for baking.");
    } else {
        serve("Temperature too low. Increase heat.");
    }
}

CheckTemperature();
